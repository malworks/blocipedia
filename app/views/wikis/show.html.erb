<h1><%= @wiki.title %></h1>
 <div class="row">
   <div class="col-md-8">
     <p><%= markdown(@wiki.body) %></p>
     <small>
       submitted by <%= @wiki.user.email %> <br>
     </small>
   </div>
   <div class="col-md-4">
     <%= link_to "Edit", edit_wiki_path(@wiki), class: 'btn btn-success' %>
     <%= link_to "Delete", @wiki, method: :delete, class: 'btn btn-danger', data: {confirm: 'Are you sure you want to delete this wiki?'} %>
     <%= link_to "New Wiki", new_wiki_path(@wiki), class: 'btn btn-success' %>
   </div>
   <div>
     <% if @wiki.private %>
        <p><%= link_to "Add Collaborators", new_wiki_collaborator_path(@wiki), class: 'btn btn-default' %></p>
     <% end %>
    </div>
 </div>
